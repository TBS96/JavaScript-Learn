// Initial string array:
// var friendsName = ["Bablu", 'Kalam', "Aman", "Salam"];
// console.log(friendsName);

// to add or push data in friendsName array to the last:
// friendsName.push("Sumit", "Pomit");
// console.log(friendsName);


// Initial number array:
var numbers = [11, 20, 22, 45, 90];
console.log(numbers);

// to add or push data in numbers array to the last:
// numbers.push(555, 412);
// console.log(numbers);

// to delete or pop from numbers array from last of array:
numbers.pop();
numbers.pop();
numbers.pop();
numbers.pop();
numbers.pop();
numbers.push(10);
console.log(numbers);